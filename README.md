### Build a VPC with Terraform Modules

Creates a VPC, a public subnet, an internet gateway, routing table,
routing table association, an AWS security group, and an EC2 instance

```
terraform init
terraform fmt
terraform validate
terraform apply
terraform destroy
```